<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <!-- Mask to bound the ripple to the pill shape (matching the foreground offset) -->
    <item android:id="@android:id/mask"
        android:bottom="3dp"
        android:right="3dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="9999dp" />
        </shape>
    </item>
    <item>
        <layer-list>
            <!-- Foreground pill layer with transparent offset to match selected state -->
            <item
                android:bottom="3dp"
                android:right="3dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/white" />
                    <stroke
                        android:width="2dp"
                        android:color="@color/graphite" />
                    <corners android:radius="9999dp" />
                </shape>
            </item>
        </layer-list>
    </item>
</ripple>